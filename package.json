{
	"name": "react-video-render-prop",
	"version": "0.1.0",
	"private": true,
	"dependencies": {
		"react": "^16.0.0",
		"react-dom": "^16.0.0",
		"react-scripts": "1.0.14"
	},
	"scripts": {
		"start": "react-scripts start",
		"build": "react-scripts build",
		"test": "react-scripts test --env=jsdom src/*test*",
		"testcov": "react-scripts test --env=jsdom --coverage && codecov",
		"eject": "react-scripts eject",
		"package": "rimraf coverage && rimraf build && rimraf node_modules"
	},
	"devDependencies": {
		"codecov": "2.3.0",
		"react-test-renderer": "16.0.0"
	}
}
